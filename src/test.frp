TDNT 
    table = tableau de 10 reel 
    table2 = tableau de 20 table
(* the table will be copied *)
fonction who(x: table): entier
TDOL
    i: entier
debut
    pour i de 0 a 9 faire 
        ecrire(x[i])
        x[i] := i 
        ecrire(x[i])
    fin pour
    retourner 0 
fin 
TDOG
    i: entier
    who: entier
    test: table 
    bruh: table2
debut
    pour i de 0 a 9 faire
        test[i] := -i*i
        ecrire(test[i])
    fin pour 
    who := who(test)
    pour i de 0 a 9 faire
        ecrire(test[i])
    fin pour 
    ecrire(test[ent(test[2])])
fin
